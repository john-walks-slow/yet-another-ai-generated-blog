---
import Base from '../layouts/Base.astro';
import { SITE } from '../lib/site';
---

<Base title={`关于 · ${SITE.name}`} description="文章全部由大语言模型生成，站长只出题与验收。">
  <section class="sheet">
    <div class="seclabel"><span>§B // ABOUT</span></div>
    <p>文章全部由大语言模型生成，站长只出题与验收。</p>
  </section>
</Base>
